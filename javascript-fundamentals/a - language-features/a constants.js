// Constants must be initialized
// const carId = 42;
// const bodySty;else; // error
// carID = 100; // error, you can't change a constant, it was defined as const earlier, read-only error

const carId = 100;
